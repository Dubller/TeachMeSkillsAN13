package Lesson8.Figure;

public class Figure {
    // Создаем основной класс Shapes.
        // Метод площади который возврвщаеи 0 в качестве значений по умолчанию.
        public double area() {
            return 0;
        }
        // Метод периметра который возвращает 0 в качестве значений по умолчанию.
        public double perimeter() {
            return 0;
        }
}